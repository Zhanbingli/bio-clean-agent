dataset:
  dataset_id: rna_demo
  dataset_type: transcriptomics
  raw_paths:
    - data/rna_demo_counts.csv
  matrix_format: counts
  metadata_path: data/rna_demo_metadata.csv
output_dir: outputs/rna_demo
parameters:
  min_counts: 5
  min_cells: 2
report_dir: reports
